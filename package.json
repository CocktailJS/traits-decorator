{
  "name": "cocktail.next",
  "version": "0.0.0",
  "description": "traits and mixins as decorators",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "compile": "echo \"compiling...\" && babel -d lib/ src/",
    "postinstall": "echo \"post-install...\" && npm run compile",
    "babel": "babel-node "
  },
  "author": "Maximiliano Fierro",
  "license": "MIT",
  "devDependencies": {
    "babel": "^5.6.6"
  }
}
